apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: statestore
spec:
  type: state.sqlserver
  version: v1
  metadata:
  - name: connectionString
    value: Server=localhost; User Id=SA; Password=<YourStrong@Passw0rd>; Trusted_Connection=True;
